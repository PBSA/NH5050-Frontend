stages:
  - build
  - deploy

build:
  image: node:12 
  stage: build
  script:
    - npm install
  cache:
    paths:
      - node_modules
  artifacts:
    paths:
      - /*
  tags:
    - docker
  
deployToDev:
  stage: deploy
  script:
    - cp -R ../NH5050-frontend $PROJECT_DIR
    - deployFrontend
  only:
    - develop
  tags:
    - 5050dev
